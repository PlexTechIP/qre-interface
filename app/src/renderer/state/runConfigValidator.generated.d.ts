// Types for the generated, eval-free RunConfig validator (see
// scripts/compileSchemas.mjs). The .js is produced by Ajv's standaloneCode.
import type { ValidateFunction } from "ajv";

export const validate: ValidateFunction;
declare const _default: ValidateFunction;
export default _default;
